
const handler = async (m, { conn }) => {
  m.reply("el puto más negro del mundo es la zorra prostituta de anuar es un puto negro de mrd ");
};

handler.command = 'masnegro', /^(masnegro|\.masnegro)$/i;

export default handler;